{
  "description": "A required attribute whose value is null is present",
  "spec": [
    "spec.md#schema-driven-processing"
  ],
  "op": "decode",
  "notes": "The spec's guidance that applications should treat a null attribute as absent is about values, while schema processing only looks for a definition.",
  "schema": {
    "mode": "exhaustive",
    "attributes": [
      {
        "name": "a",
        "required": true
      }
    ]
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "null": "dynamic"
        }
      },
      "blocks": []
    }
  }
}

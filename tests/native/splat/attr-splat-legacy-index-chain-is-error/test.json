{
  "description": "Chained legacy indexes after an attribute-only splat are an error",
  "spec": [
    "hclsyntax/spec.md#splat-operators",
    "hclsyntax/spec.md#index-operator"
  ],
  "op": "parse",
  "reference_error": "Use the proper index syntax with a full splat expression",
  "expect": {
    "valid": false
  }
}

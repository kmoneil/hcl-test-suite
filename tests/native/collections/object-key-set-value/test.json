{
  "description": "A key that evaluates to a set is an error",
  "spec": [
    "hclsyntax/spec.md#collection-values",
    "spec.md#collection-types"
  ],
  "op": "eval",
  "features": [
    "typed-values"
  ],
  "notes": "Attribute names are strings, and there is no conversion from a set to a string.",
  "variables": {
    "k": {
      "set": [
        {
          "string": "x"
        }
      ],
      "element_type": "string"
    }
  },
  "reference_error": "string required, but have set of string",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

{
  "description": "A for directive whose value variable is a string is a syntax error",
  "spec": [
    "hclsyntax/spec.md#template-for-directive"
  ],
  "op": "parse",
  "reference_error": "requires value variable name after comma",
  "expect": {
    "valid": false
  }
}

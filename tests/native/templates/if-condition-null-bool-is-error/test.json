{
  "description": "A null bool as an if condition is an error",
  "spec": [
    "hclsyntax/spec.md#template-if-directive",
    "hclsyntax/spec.md#conditional-operator"
  ],
  "op": "eval",
  "features": [
    "typed-values"
  ],
  "status": "disputed",
  "notes": "The spec's conditional operator needs a boolean predicate and only says what true and false select; it doesn't say whether a null bool is an error. hashicorp/hcl reports \"Null condition\" in ConditionalExpr.Value (hclsyntax/expression.go), the node an if directive is parsed into.",
  "variables": {
    "v": {
      "null": "bool"
    }
  },
  "reference_error": "Null condition",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

{
  "description": "The strings true and false are converted to bool when used as an if condition",
  "spec": [
    "hclsyntax/spec.md#for-expressions",
    "spec.md#type-conversions-and-unification",
    "spec.md#primitive-type-conversions"
  ],
  "op": "eval",
  "notes": "The condition must be a bool. The information model attempts automatic conversion when a value doesn't have the type an operation expects, and defines the strings true and false as the bool values.",
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "tuple": [
            {
              "string": "true"
            }
          ]
        }
      },
      "blocks": []
    }
  }
}

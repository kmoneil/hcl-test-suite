{
  "description": "An object for expression can't iterate over a string",
  "spec": [
    "hclsyntax/spec.md#for-expressions"
  ],
  "op": "eval",
  "reference_error": "value of type string cannot be used as the collection",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

{
  "description": "A for expression without any iteration variable is a syntax error",
  "spec": [
    "hclsyntax/spec.md#for-expressions"
  ],
  "op": "parse",
  "notes": "hashicorp/hcl lets in name a variable (see for-variable-named-in), so it reads the first in as the variable and then reports the missing in keyword. Either way the input has one mistake.",
  "reference_error": "requires the 'in' keyword",
  "expect": {
    "valid": false
  }
}

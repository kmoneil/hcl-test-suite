{
  "description": "A for expression can't iterate over a number",
  "spec": [
    "hclsyntax/spec.md#for-expressions"
  ],
  "op": "eval",
  "reference_error": "value of type number cannot be used as the collection",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

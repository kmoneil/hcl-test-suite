{
  "description": "The in keyword must be a separate token, so inxs is an identifier and not in followed by xs",
  "spec": [
    "hclsyntax/spec.md#for-expressions",
    "hclsyntax/spec.md#keywords"
  ],
  "op": "parse",
  "notes": "An identifier token extends over all following identifier characters, so inxs is one identifier and the in keyword is missing.",
  "reference_error": "requires the 'in' keyword",
  "expect": {
    "valid": false
  }
}

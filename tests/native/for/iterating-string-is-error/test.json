{
  "description": "A for expression can't iterate over the characters of a string",
  "spec": [
    "hclsyntax/spec.md#for-expressions"
  ],
  "op": "eval",
  "reference_error": "value of type string cannot be used as the collection",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

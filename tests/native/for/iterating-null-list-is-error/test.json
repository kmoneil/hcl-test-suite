{
  "description": "A for expression can't iterate over a null list",
  "spec": [
    "hclsyntax/spec.md#for-expressions",
    "spec.md#null-values"
  ],
  "op": "eval",
  "features": [
    "typed-values"
  ],
  "status": "disputed",
  "notes": "The spec lists tuple, object, list, map and set types as iterable but doesn't say what iterating a null value does; treating it as empty would also be reasonable. hashicorp/hcl reports an error for any null collection: ForExpr.Value in hclsyntax/expression.go checks IsNull before anything else.",
  "variables": {
    "l": {
      "null": [
        "list",
        "string"
      ]
    }
  },
  "reference_error": "Iteration over null value",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

{
  "description": "A splat over an unknown tuple of objects gives an unknown tuple of the attribute types",
  "spec": [
    "hclsyntax/spec.md#splat-operators",
    "spec.md#unknown-values-and-the-dynamic-pseudo-type"
  ],
  "op": "eval",
  "features": [
    "typed-values",
    "unknown-values"
  ],
  "notes": "This follows the splat's own typing, which for a tuple source is a tuple of each traversal's result, as for a known tuple. The spec also calls a splat approximately equivalent to a for expression, and a for expression over an unknown collection gives the dynamic value, but it doesn't say that the approximation extends to unknowns.",
  "variables": {
    "u": {
      "unknown": [
        "tuple",
        [
          [
            "object",
            {
              "a": "string"
            }
          ],
          [
            "object",
            {
              "a": "number"
            }
          ]
        ]
      ]
    }
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "unknown": [
            "tuple",
            [
              "string",
              "number"
            ]
          ]
        }
      },
      "blocks": []
    }
  }
}

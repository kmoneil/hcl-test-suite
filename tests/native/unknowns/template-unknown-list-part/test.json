{
  "description": "A template with an unknown list part gives an unknown string",
  "spec": [
    "hclsyntax/spec.md#template-interpolations",
    "hclsyntax/spec.md#templates",
    "spec.md#unknown-values-and-the-dynamic-pseudo-type"
  ],
  "op": "eval",
  "features": [
    "typed-values",
    "unknown-values"
  ],
  "status": "disputed",
  "notes": "The spec says an interpolation whose result can't be converted to a string is an error, and no list converts to a string. hashicorp/hcl returns an unknown string because TemplateExpr.Value (hclsyntax/expression_template.go) skips the conversion for unknown parts.",
  "variables": {
    "u": {
      "unknown": [
        "list",
        "string"
      ]
    }
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "unknown": "string"
        }
      },
      "blocks": []
    }
  }
}

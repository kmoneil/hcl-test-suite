{
  "description": "A splat over an unknown number gives the dynamic value",
  "spec": [
    "hclsyntax/spec.md#splat-operators",
    "spec.md#unknown-values-and-the-dynamic-pseudo-type"
  ],
  "op": "eval",
  "features": [
    "typed-values",
    "unknown-values"
  ],
  "notes": "The unknown number might be null, which gives an empty tuple, or not, which gives a one-element tuple, so not even the type is known.",
  "variables": {
    "u": {
      "unknown": "number"
    }
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "unknown": "dynamic"
        }
      },
      "blocks": []
    }
  }
}

{
  "description": "A template that only interpolates the dynamic value gives the dynamic value",
  "spec": [
    "hclsyntax/spec.md#template-interpolation-unwrapping",
    "hclsyntax/spec.md#templates",
    "spec.md#unknown-values-and-the-dynamic-pseudo-type"
  ],
  "op": "eval",
  "features": [
    "typed-values",
    "unknown-values"
  ],
  "notes": "Unwrapping returns the interpolation's result verbatim, which takes priority over the general rule that a template with a dynamic part gives an unknown string. An implementation whose unknown values are all dynamic would pass regardless, hence typed-values.",
  "variables": {
    "d": {
      "unknown": "dynamic"
    }
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "unknown": "dynamic"
        }
      },
      "blocks": []
    }
  }
}

{
  "description": "Legacy index syntax cannot be chained, because 0.0 reads as a number",
  "spec": [
    "hclsyntax/spec.md#index-operator"
  ],
  "op": "parse",
  "expect": {
    "valid": false
  }
}

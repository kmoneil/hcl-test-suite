{
  "description": "A string with a leading plus sign converts to number",
  "spec": [
    "spec.md#primitive-type-conversions",
    "hclsyntax/spec.md#arithmetic-operators"
  ],
  "op": "eval",
  "status": "disputed",
  "notes": "The spec converts a string to a number by reversing number to string conversion, which never writes a plus sign. hashicorp/hcl accepts one because go-cty parses the string with big.ParseFloat (ParseNumberVal in cty/value_init.go).",
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "number": "5"
        }
      },
      "blocks": []
    }
  }
}

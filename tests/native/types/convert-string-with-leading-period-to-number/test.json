{
  "description": "A string starting with a period converts to number",
  "spec": [
    "spec.md#primitive-type-conversions",
    "hclsyntax/spec.md#arithmetic-operators"
  ],
  "op": "eval",
  "status": "disputed",
  "notes": "The spec converts a string to a number by reversing number to string conversion, which always writes the integer part, so \".5\" isn't in the expected syntax. hashicorp/hcl accepts it because go-cty parses the string with big.ParseFloat (ParseNumberVal in cty/value_init.go).",
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "number": "0.5"
        }
      },
      "blocks": []
    }
  }
}

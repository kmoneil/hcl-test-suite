{
  "description": "Tuples of different lengths unify to a list",
  "spec": [
    "spec.md#type-unification",
    "hclsyntax/spec.md#conditional-operator"
  ],
  "op": "eval",
  "features": [
    "typed-values"
  ],
  "status": "disputed",
  "notes": "The spec unifies tuple types only when they have the same length, and a tuple can't convert to a tuple of another length, so this should fail. hashicorp/hcl unifies tuples of different lengths to a list of their unified element types (unifyTupleTypesToList in cty/convert/unify.go).",
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "list": [
            {
              "number": "1"
            }
          ],
          "element_type": "number"
        }
      },
      "blocks": []
    }
  }
}

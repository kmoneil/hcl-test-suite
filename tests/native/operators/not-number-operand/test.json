{
  "description": "! rejects a number as its operand",
  "spec": [
    "hclsyntax/spec.md#logic-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for unary operand: bool required, but have number",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

{
  "description": "* rejects an object as its left operand",
  "spec": [
    "hclsyntax/spec.md#arithmetic-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for left operand: number required, but have object",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

{
  "description": "% rejects a tuple as its right operand",
  "spec": [
    "hclsyntax/spec.md#arithmetic-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for right operand: number required, but have tuple",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

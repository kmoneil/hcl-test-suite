{
  "description": "&& rejects an object as an operand",
  "spec": [
    "hclsyntax/spec.md#logic-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for right operand: bool required, but have object",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

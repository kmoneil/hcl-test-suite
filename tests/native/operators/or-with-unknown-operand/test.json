{
  "description": "|| with an unknown operand and a true operand",
  "spec": [
    "hclsyntax/spec.md#logic-operators"
  ],
  "op": "eval",
  "features": [
    "unknown-values"
  ],
  "variables": {
    "u": {
      "unknown": "bool"
    }
  },
  "status": "disputed",
  "notes": "The spec says the result is unknown if either operand is unknown. The reference implementation returns true, because true || anything is true (OpLogicalOr in hclsyntax/expression_ops.go).",
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "bool": true
        }
      },
      "blocks": []
    }
  }
}

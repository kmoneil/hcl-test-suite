{
  "description": "&& rejects a number as an operand",
  "spec": [
    "hclsyntax/spec.md#logic-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for left operand: bool required, but have number",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

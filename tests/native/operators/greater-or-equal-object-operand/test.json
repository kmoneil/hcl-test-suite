{
  "description": ">= rejects an object as its right operand",
  "spec": [
    "hclsyntax/spec.md#comparison-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for right operand: number required, but have object",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

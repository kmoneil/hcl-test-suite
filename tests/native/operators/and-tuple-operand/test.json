{
  "description": "&& rejects a tuple as an operand",
  "spec": [
    "hclsyntax/spec.md#logic-operators"
  ],
  "op": "eval",
  "reference_error": "Unsuitable value for left operand: bool required, but have tuple",
  "expect": {
    "valid": false,
    "phase": "eval"
  }
}

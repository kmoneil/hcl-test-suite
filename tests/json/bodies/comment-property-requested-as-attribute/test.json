{
  "description": "A schema that requests an attribute named // gets the property named //",
  "spec": [
    "json/spec.md#structural-elements"
  ],
  "op": "decode",
  "features": [
    "json-syntax"
  ],
  "status": "disputed",
  "notes": "The spec says a property named // in an object representing a body is parsed and ignored. hashicorp/hcl only skips it when it isn't requested: (*body).PartialContent in json/structure.go matches properties against the schema before Content skips unmatched // properties, so a schema that requests an attribute named // gets it.",
  "schema": {
    "mode": "exhaustive",
    "attributes": [
      {
        "name": "//"
      }
    ]
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "//": {
          "string": "note"
        }
      },
      "blocks": []
    }
  }
}

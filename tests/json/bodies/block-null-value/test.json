{
  "description": "A null value for a block type defines no blocks",
  "spec": [
    "json/spec.md#blocks"
  ],
  "op": "decode",
  "features": [
    "json-syntax"
  ],
  "status": "disputed",
  "notes": "The spec says the value that defines blocks is either a JSON object or a JSON array of objects, so null isn't allowed. hashicorp/hcl skips a null value ((*body).unpackBlock in json/structure.go).",
  "schema": {
    "mode": "exhaustive",
    "blocks": [
      {
        "type": "b",
        "body": {
          "mode": "dynamic-attributes"
        }
      }
    ]
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {},
      "blocks": []
    }
  }
}

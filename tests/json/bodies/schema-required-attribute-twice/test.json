{
  "description": "A schema requesting the same required attribute twice gives that attribute once",
  "spec": [
    "spec.md#schema-driven-processing"
  ],
  "op": "decode",
  "features": [
    "json-syntax"
  ],
  "status": "disputed",
  "notes": "The spec says a schema that requests the same attribute name twice is an error. hashicorp/hcl doesn't check schemas, and for the JSON syntax keeps one schema per attribute name ((*body).PartialContent in json/structure.go), so unlike the native syntax it doesn't report the attribute missing.",
  "schema": {
    "mode": "exhaustive",
    "attributes": [
      {
        "name": "a",
        "required": true
      },
      {
        "name": "a",
        "required": true
      }
    ]
  },
  "expect": {
    "valid": true,
    "body": {
      "attributes": {
        "a": {
          "number": "1"
        }
      },
      "blocks": []
    }
  }
}

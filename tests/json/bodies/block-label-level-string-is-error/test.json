{
  "description": "A string where a label level is expected is an error",
  "spec": [
    "json/spec.md#blocks"
  ],
  "op": "decode",
  "features": [
    "json-syntax"
  ],
  "schema": {
    "mode": "exhaustive",
    "blocks": [
      {
        "type": "b",
        "labels": [
          "name"
        ],
        "body": {
          "mode": "dynamic-attributes"
        }
      }
    ]
  },
  "reference_error": "Either a JSON object or JSON array of objects is required here, to specify name labels for this block",
  "expect": {
    "valid": false,
    "phase": "schema"
  }
}
